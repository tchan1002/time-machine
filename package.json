{
  "name": "md-journal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static site generator for Markdown journal entries with compare and stats pages.",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "gh-pages -d dist",
    "clean:entries": "node scripts/clean.js"
  },
  "dependencies": {
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}

